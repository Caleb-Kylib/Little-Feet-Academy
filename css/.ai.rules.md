# AI Project Rules

1. Always use semantic HTML5 structure for new sections.  
2. Ensure accessibility (labels, ARIA if needed, form validation).  
3. CSS should be responsive and consistent with the site’s existing theme.  
4. Avoid inline styles; keep all styling in `style.css`.  
5. Optimize for readability and maintainability before performance.  
